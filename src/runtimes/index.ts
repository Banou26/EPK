import chrome from './chrome.ts'
import { of } from 'rxjs'

export default options => {
  const runtimes = new Map()

  return of(
    runtimeName => {
      if (runtimeName === 'chrome') {
        
      }
    }
  )
}
