import { test } from '../../dist/test.js'
import { of } from 'rxjs'

// todo: starts researching & implementing e2e testing to properly test EPK for observables

test('run observables', () => of(true))
